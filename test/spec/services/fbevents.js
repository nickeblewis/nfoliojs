'use strict';

describe('Service: fbevents', function () {

  // load the service's module
  beforeEach(module('farnboroughyoApp'));

  // instantiate service
  var fbevents;
  beforeEach(inject(function (_fbevents_) {
    fbevents = _fbevents_;
  }));

  //it('should do something', function () {
  //  expect(!!fbevents).toBe(true);
  //});

});
