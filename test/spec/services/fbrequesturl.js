'use strict';

describe('Service: fbrequesturl', function () {

  // load the service's module
  beforeEach(module('farnboroughyoApp'));

  // instantiate service
  var fbrequesturl;
  beforeEach(inject(function (_fbrequesturl_) {
    fbrequesturl = _fbrequesturl_;
  }));

  //it('should do something', function () {
  //  expect(!!fbrequesturl).toBe(true);
  //});

});
