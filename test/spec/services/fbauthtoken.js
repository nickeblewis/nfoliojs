'use strict';

describe('Service: fbauthtoken', function () {

  // load the service's module
  beforeEach(module('farnboroughyoApp'));

  // instantiate service
  var fbauthtoken;
  beforeEach(inject(function (_fbauthtoken_) {
    fbauthtoken = _fbauthtoken_;
  }));

  //it('should do something', function () {
  //  expect(!!fbauthtoken).toBe(true);
  //});

});
